# Superseded by verify-org-tangle-sync.ps1 — use: tangle-check
Write-Host 'check-config-drift.ps1 is retired. Use: tangle-check' -ForegroundColor Yellow
Write-Host '  Read-only:  tangle-check' -ForegroundColor DarkGray
Write-Host '  Fix drift:  tangle-check -Fix' -ForegroundColor DarkGray
exit 1
