return {
  colorscheme = "catppuccin-mocha",
  diagnostics = {
    virtual_text = true,
    underline = true,
  },
  polish = function()
    -- require("notify").setup({
    --   background_colour = "#ffffff",
    -- })
  end,
}
